# Application Layer — orchestrating existing components without business logic duplication.
# Services in this package wrap real domain entry points and pipeline builders.
# No new engines, no mock values, no fallback fake logic.
